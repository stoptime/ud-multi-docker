apiVersion: v1
kind: Service
metadata:
  name: postgres-loadbalancer-service
spec:
  type: LoadBalancer
  selector:
    component: postgres
  ports:
    - port: 5001
      targetPort: 5001
      nodePort: 5001